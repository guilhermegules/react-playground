# React From Scratch

Mini React application for demonstration of initial configuration

- Run `npm install` for install all dependencies
- Run `npm start` for start app in development mode
- Run `npm run build` for generate a production ready application
