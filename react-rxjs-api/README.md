# React + RxJS API integration

This project implements a simple API integration with RxJS and React

## What was used

- React 18.2.0+
- RxJS 7.8.1+
- TypeScript 5.2.2+

## How Run

- Install dependencies `npm install` or `pnpm install`;
- Run application in dev mode `npm run dev` or `pnpm dev`;
